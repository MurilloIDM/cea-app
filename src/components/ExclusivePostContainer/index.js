// import React, { useState, useEffect } from "react";
import { ScrollView } from "react-native";

// import styles from "./styles";

const ExclusivePostContainer = () => {


  return (
    <ScrollView>
      {children}
    </ScrollView>
  );

}

export default ExclusivePostContainer;
